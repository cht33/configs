path
```
.vim
├── README.md
├── autoload
│   └── plug.vim
├── coc-settings.json
├── plugged
└── vimrc_plug
```
command
```
mkdir ~/.vim
cp -r ./* ~/.vim/
cp .vimrc ~/
```

注意 `coc.nvim` 所需的 `node` 最低版本